{
  "name": "rsk-openapi-ui",
  "version": "0.0.7",
  "description": "Open api viewer",
  "main": "index.js",
  "scripts": {
    "build": "npm run clean && npm run public && npx webpack --config webpack.config.js",
    "clean": "rm -rf dist && mkdir -p dist",
    "public": "cp -r public/* ./dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "emi",
  "license": "MIT",
  "dependencies": {
    "swagger-ui": "^3.38.0"
  },
  "devDependencies": {
    "css-loader": "^6.7.3",
    "html-webpack-plugin": "^5.5.0",
    "style-loader": "^3.3.1",
    "webpack": "^5.75.0",
    "webpack-cli": "^5.0.1"
  }
}
